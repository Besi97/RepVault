interface PaginatedList {
    pageInfo: PageInfo!
    totalCount: Int!
}

type PageInfo {
    pageIndex: Int!
    hasNextPage: Boolean!
}

input PaginationInput {
    count: Int!
    pageIndex: Int
}
